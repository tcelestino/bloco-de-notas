{
  "name": "notes",
  "version": "1.0.0",
  "scripts": {
    "clean": "rm -rf ./_site",
    "build": "npm run clean && eleventy --pathprefix=notes",
    "dev": "DEBUG=Eleventy* eleventy --serve --watch",
    "start": "eleventy --serve --watch"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tcelestino/notes.git"
  },
  "author": "tcelestino",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "0.12.1",
    "@11ty/eleventy-plugin-syntaxhighlight": "3.0.4",
    "prettier": "2.2.1"
  }
}
